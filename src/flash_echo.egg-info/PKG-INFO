Metadata-Version: 2.4
Name: flash-echo
Version: 0.1.0
Summary: Low-latency filler words decision service for real-time voice interactions.
Requires-Python: >=3.10
Description-Content-Type: text/markdown
Requires-Dist: fastapi>=0.111
Requires-Dist: pydantic>=2.7
Requires-Dist: uvicorn[standard]>=0.30
Provides-Extra: dev
Requires-Dist: pytest>=8.2; extra == "dev"
Requires-Dist: httpx>=0.27; extra == "dev"
Provides-Extra: ml
Requires-Dist: jinja2>=3.1; extra == "ml"
Requires-Dist: onnxruntime>=1.18; extra == "ml"
Requires-Dist: optimum[onnxruntime]>=1.24; extra == "ml"
Requires-Dist: transformers>=4.41; extra == "ml"
Requires-Dist: torch>=2.3; extra == "ml"

# Flash Echo

Flash Echo 是一个用于实时语音交互的低延迟语气词决策服务。

该服务接收当前用户的 `query` 和 `persona_tag`，预测是否需要播放语气词前缀，并返回一个与 persona 对应的前缀，供下游 TTS 与主 LLM 流水线作为首段语音内容使用。

## 项目结构

```text
configs/                 静态服务与模板配置
data/                    原始、处理后及疑难样本数据集
docs/                    产品需求与实现方案
models/                  导出的模型包与元数据
scripts/                 数据、模板、训练与导出工具
src/filler_words/        运行时包
tests/                   单元测试与 API 测试
```

## 快速开始

```bash
python -m venv .venv
source .venv/bin/activate
pip install -e ".[dev]"
uvicorn filler_words.app:create_app --factory --reload
```

请求示例：

```bash
curl -X POST http://127.0.0.1:8000/v1/filler \
  -H "content-type: application/json" \
  -d '{"request_id":"req-001","query":"你怎么看 AI 对教育行业的影响？","persona_tag":"male_white_collar"}'
```

## 模型集成

当前运行时默认使用相对保守的 `HeuristicClassifier`，以便在 ONNX 模型包尚未就绪时服务仍可运行。当 `models/filler-cls-v1/` 中包含导出的模型、tokenizer 文件、标签映射和版本元数据后，可将其替换为基于 ONNX、并实现于 `BaseClassifier` 之上的版本。
